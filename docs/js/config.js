/* Конфиг Mini App: бэкенд (Railway) + работа без него */
window.APP_CONFIG = {
  // API в облаке (бот, оплата, точный восходящий, живой прогноз)
  API_BASE: "https://svetly-lenormand-production.up.railway.app",
  // таймаут, после которого работаем локально
  API_TIMEOUT_MS: 8000,
};
